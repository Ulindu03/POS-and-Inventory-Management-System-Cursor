# Server Port
PORT=5000
NODE_ENV=development

# Mongo
MONGODB_URI=mongodb://localhost:27017/voltzone_pos

# JWT Secrets
JWT_ACCESS_SECRET=change_me_access
JWT_REFRESH_SECRET=change_me_refresh

# App URL (frontend base for reset links)
APP_URL=http://localhost:5173

# SMTP (choose ONE of the two approaches below)
# 1. Explicit SMTP host (e.g. Gmail SMTP)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=voltzone003@gmail.com
# SMTP_PASS=your_gmail_app_password

# 2. Simple Gmail fallback (leave SMTP_HOST unset, still set user/pass)
# SMTP_USER=voltzone003@gmail.com
# SMTP_PASS=your_gmail_app_password

EMAIL_FROM=voltzone003@gmail.com

# Enable to return OTP in API (DO NOT use in production)
DEBUG_SHOW_OTP=true
ENABLE_ETHEREAL_FALLBACK=true
